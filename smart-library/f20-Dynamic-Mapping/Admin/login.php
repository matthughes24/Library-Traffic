<?php
	/*HELLO! This is AI group from 9/24/2022. I am 90% this 
	file is enitely outdated by the file located in 
	'f21-Dynamic-Mapping/Admin/login.php' so I have
	commented out the code. This should probably be deleted in the
	future because I cannot find any page that uses this file*/
	

	// include ("connect.php");
	// // check if submit is pressed, then process inputs
	// if (isset($_POST['submit']))
	// {
	// 	$Username = $conn->real_escape_string($_POST['Username']);
	// 	$Password = $conn->real_escape_string($_POST['Password']);
	// 	$stmt = $conn->query("SELECT * FROM login WHERE Username = '{$Username}' AND Password = '{$Password}' LIMIT 1");
	// 	// if a match is found, direct to admin controls page
	// 	if ($stmt->num_rows == 1)
	// 	{
	// 		echo "<script>
	// 		window.open('adminPanel.html', '_blank');
    //     	window.location.href = '/p/s21-01/smart-library/';
			
    // </script>";
	// 	}
	// 	//check that all fields are filled
	// 	else
	// 	{
	// 		echo "Error: Invalid Login Credentials.";
	
	// 		echo "<br><br><a href=\"javascript:history.go(-1)\"><button type = 'submit' name = 'retry'> Retry </button></a>";
	// 	}
	// }
	// $conn->close();
?>
